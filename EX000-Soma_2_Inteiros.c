#include <stdio.h>
#include <stdlib.h>

int main(){

    // Declaração das variáveis
    int a, b;

    // Entrada dos dados
    scanf( "%d %d", &a, &b );

    // Saída do programa
    printf( "%d + %d = %d\n", a, b, a + b );

    return 0;
}
